Version js: ec6 => ECMAScript (contains almost everything)

Tempalte Literals:
    eg:
    let a ="jai"
    let b="Anna"
    let c="Jack"
    let e = `Hello ${a}, ${b} and ${c}`;
    console.log(e);
    
    o/p: Hello jai, Anna and Jack

Arrow Fnc:
    eg:
    let sum=(x,y)=>x+y
    console.log(sum(5,10))

Map function:
    eg:
    arr.map(x => x*5);

Filter function:
    eg:
    arr.filter(x => typeof(x) === "number") (filter out the irrelvent values)

Generate Random Value:
    eg:
    Math.random() * 100 //0-100

Floor:
    eg:
    Math.floor(Math.random()*10)+1

Ceil:
    eg:
    Math.ceil(Math.random()*100)

TypeCasting:
    eg:
    parseInt(val)


Arrays:
    eg:
    arr.indexOf("val") //returns index
    arr.includes("val") //returns boolean value
    arr.length
    arr.push(val)
    ar.pop()
    arr.unshift() //add in the beginning
    arr.shift() //removes from the beginning

Reduce: (accommodate,current)
    let price=['10','20','30',40']    
    const total = prices.reduce((acc, curr) => { return acc + curr }, 0);
                                                        acc          curr
    // 0 + 10 = 10
    // 10 + 20 = 30
    // 30 + 30 = 60
    // 60 + 40 = 100
    console.log(total)

Login Page Attempt:

1. Login.html:
    -Login page with Username and password 
    -Submit button triggers an alert msg (js used)
    -Successful login takes you to another html file (sample.html)

2.Login.css:
    -Color was added to h1, p, button
    -Button was added with 'cursor' option

3.Login.js:
    -used getElementById for getting value from inputbox
    -Used if condition for checking the username and password
    -if successful, alert msg is triggered and which takes us to another html file(sample)-> window.location.href = "sample.html";
    -if unsuccessful -> invalid trigger msg

4.sample.html:
    -just a landing page eg
    -has a link to go back to login page

SPLICE:
    eg:const months = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
console.log(months.splice(2, 3, 'Aaryan')); 
console.log(months);

o/p:  
[ 'March', 'April', 'May' ]
[
  'January',   'February',
  'Aaryan',    'June',
  'July',      'August',
  'September', 'October',
  'November',  'December'
]

splice(a,b,c)
a->index
b->no of elements
c->replaced in place of a-b

